# .igntui.cfg.toml — generated by igntui; safe to commit.
# Re-running igntui in this directory restores this selection.

schema_version = 1
generated_at = "2026-09-09T01:52:46Z"
igntui_version = "0.5.0"

[selection]
templates = [
    "java",
]
search_mode = "fuzzy"

[output]
path = ".gitignore"
preserve_user_edits = true
